[build]
  command = "pnpm build"
  publish = "dist/public"
  functions = "dist"

[context.production]
  command = "pnpm build"
  publish = "dist/public"

# SPA routing: redirect all non-file requests to index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
